{
  "name": "FRR Router",
  "version": "1.0.0",
  "description": "Ubuntu 22.04 with FRRouting (OSPF/BGP/Zebra), IP forwarding, and systemd timer that auto-detects new dataplane interfaces every 15 seconds.",
  "image": "default_ubuntu_22",
  "builtin": true,
  "boot_config": {
    "uploads": [],
    "commands": [
      {
        "id": "1",
        "command": "sudo apt-get update && sudo apt-get install -y frr",
        "order": 0
      },
      {
        "id": "2",
        "command": "sudo sed -i 's/^zebra=no/zebra=yes/' /etc/frr/daemons && sudo sed -i 's/^ospfd=no/ospfd=yes/' /etc/frr/daemons && sudo sed -i 's/^bgpd=no/bgpd=yes/' /etc/frr/daemons",
        "order": 1
      },
      {
        "id": "3",
        "command": "sudo sysctl -w net.ipv4.ip_forward=1 && echo 'net.ipv4.ip_forward = 1' | sudo tee /etc/sysctl.d/99-ip-forward.conf > /dev/null",
        "order": 2
      },
      {
        "id": "4",
        "command": "sudo systemctl restart frr",
        "order": 3
      },
      {
        "id": "5",
        "command": "sudo tee /usr/local/bin/frr-auto-connect.sh > /dev/null <<'SCRIPT'\n#!/bin/bash\nKNOWN_FILE=\"/var/run/frr-known-interfaces\"\ntouch \"$KNOWN_FILE\"\nfor iface in $(ip -o link show | awk -F': ' '{print $2}' | grep -v -E '^(lo|eth0|ens3|docker|br|ovs|veth|virbr)'); do\n    if ! grep -qx \"$iface\" \"$KNOWN_FILE\"; then\n        ip link set \"$iface\" up 2>/dev/null || true\n        echo \"$iface\" >> \"$KNOWN_FILE\"\n        logger \"frr-auto-connect: detected new interface $iface\"\n    fi\ndone\nSCRIPT\nsudo chmod +x /usr/local/bin/frr-auto-connect.sh",
        "order": 4
      },
      {
        "id": "6",
        "command": "sudo tee /etc/systemd/system/frr-auto-connect.service > /dev/null <<'EOF'\n[Unit]\nDescription=FRR Auto-Connect Dataplane Interfaces\nAfter=frr.service\n[Service]\nType=oneshot\nExecStart=/usr/local/bin/frr-auto-connect.sh\nEOF\nsudo tee /etc/systemd/system/frr-auto-connect.timer > /dev/null <<'EOF'\n[Unit]\nDescription=FRR Auto-Connect Timer\n[Timer]\nOnBootSec=10\nOnUnitActiveSec=15\n[Install]\nWantedBy=timers.target\nEOF\nsudo systemctl daemon-reload && sudo systemctl enable --now frr-auto-connect.timer",
        "order": 5
      }
    ],
    "network": []
  }
}
